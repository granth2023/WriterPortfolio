import { useRouter } from 'next/router';

const LeftArrow = () => {
    const router = useRouter();

    const handleClick = () => {
        router.push('/BluePage');
    };
    return (
        <div className=
    )
}